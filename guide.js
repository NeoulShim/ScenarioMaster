const modal=document.querySelector('#picture');const content=document.querySelector('#picture-content');let opener;document.querySelectorAll('[data-enlarge]').forEach(link=>link.addEventListener('click',event=>{event.preventDefault();opener=link;const view=document.createElement('div');view.className=link.className;view.append(link.querySelector('img').cloneNode(true));content.replaceChildren(view);modal.showModal();}));modal.querySelector('button').addEventListener('click',()=>modal.close());modal.addEventListener('close',()=>opener?.focus());modal.addEventListener('click',event=>{if(event.target===modal)modal.close();});